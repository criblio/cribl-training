activeId: default
rulesets:
  default:
    conf:
      functions:
        - filter: "true"
          id: eval
          description: Default Mappings
          disabled: false
          final: true
          conf:
            add:
              - name: groupId
                value: "'default'"
