{
    "title": "",
    "type": "object",
    "properties": {
        "srcField": {
            "title": "Source Field",
            "description": "Field which contains text to be parsed",
            "type": "string",
            "default": "_raw"
        },
        "dstField": {
            "title": "Destination Field",
            "description": "Where to store the parsed object",
            "type": "string",
            "default": "__parsed"
        },
        "ignoreFields": {
            "title": "Ignore Fields",
            "description": "Field Names to not include in the output if parsed",
            "type": "array",
            "items": {
                "type": "string"
            }
        }
    }
}